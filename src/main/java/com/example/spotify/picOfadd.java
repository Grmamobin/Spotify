package com.example.spotify;

import com.example.spotify.DataBase.Playlist;
import javafx.event.ActionEvent;
import javafx.fxml.FXML;
import javafx.scene.control.Button;
import javafx.scene.image.Image;

public class picOfadd {

    @FXML
    private Button click;

    @FXML
    void click(ActionEvent event) {

    }
    void setSecondData(Playlist playlist){

    /*    playlist.setText(playlist.getTitle());
        genre.setText(artist.getGenre());
        instagram.setText(artist.getInstagram());
        twitter.setText(artist.getTwitter());*/
    }

}
